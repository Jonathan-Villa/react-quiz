import { makeStyles } from "@material-ui/core";

export const useTabStyles = makeStyles(() => ({ 
    root: {
        flexGrow:"1",
    },
    appBar:{ 
        background: "#ffff",
        color:"black"
    }

}));
